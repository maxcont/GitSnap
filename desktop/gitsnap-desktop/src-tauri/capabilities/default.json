{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Finestra principale (carica Streamlit in dev)",
  "windows": ["main"],
  "permissions": ["core:default"]
}
